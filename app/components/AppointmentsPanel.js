export default function AppointmentsPanel({ bookings, onClose, onReschedule, onCancel }) {
  const appointments = bookings.flatMap((booking) => (booking.sessions || []).filter((session) => session.status === 'Confirmed').map((session) => ({ ...session, booking })));
  return <section className="account-panel"><button className="back" onClick={onClose}>Back to booking</button><h2>My appointments</h2><p className="subtext">Appointments confirmed by our operations team appear here.</p>{appointments.length ? <div className="booking-list">{appointments.map((item) => <article className="booking-card" key={`${item.booking.reference}-${item.number}`}><div><span className="status confirmed">Confirmed by Portea</span><h3>{item.booking.service}</h3><p>Session {item.number} · {item.date} at {item.time}</p><p>{item.booking.patient}</p></div><div className="booking-meta"><strong>{item.booking.reference}</strong><button type="button" className="action-button reschedule-button" onClick={() => onReschedule(item.booking.reference, item.number, item)}>Reschedule</button><button type="button" className="action-button cancel-button" onClick={() => onCancel(item.booking.reference, item.number)}>Cancel appointment</button></div></article>)}</div> : <div className="empty-state">No appointments confirmed yet. Our team will show them here.</div>}</section>;
}
